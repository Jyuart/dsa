// animation speed values are inverted for the range input to work the expected way: slower -> faster
export const MIN_ANIMATION_SPEED = 100;
export const MAX_ANIMATION_SPEED = 1500;
export const DEFAULT_ANIMATION_SPEED = 1400;

export const MIN_ARRAY_VALUE = 5;
export const MAX_ARRAY_VALUE = 50;
